// Re-exports from styles/patterns.js for backward compatibility.
// Map-registration logic (registerPatternImages) now lives in the layer adapter.
export {
  hasPattern,
  getPatternInnerContent,
  getPatternImageId,
  getKeyPatternPaths,
  rasterisePattern
} from './styles/patterns.js'
